import React from 'react'
import {View} from 'react-native'
import ChatList from '../../component/list/chat'

const ChatListScreen = (props) => {

    return (
        <View>
            <ChatList/>
        </View>
    );
}


export default ChatListScreen;